# holbertonschool-core-engineering
This repository contains Python fundamentals exercises focused on:

- Python interpreter vs scripts
- Expressions vs statements
- File execution
- pip and package management
- Virtual environments (venv)
- PEP8 compliance (pycodestyle)

---
## ⚙️ Requirements

- Python 3.8+
- Ubuntu 20.04 LTS
- pycodestyle (PEP8 checker)
- venv for dependency isolation

---

## 🚀 How to run a script

Make the file executable:

```bash
chmod +x namefile.py

📦 Virtual environment setup
python3 -m venv venv
source venv/bin/activate
pip install pycodestyle

🎯 Style check

Activate virtual environment:
source venv/bin/activate

Run:
pycodestyle structured_output.py

👤 Author

Rawan - Holberton School
